# YAPPIE Website

## Разработка сайтов, веб-приложений и чат-ботов для бизнеса

Сайт компании YAPPIE - команды AI-разработчиков.

## 🚀 Деплой на GitHub Pages

### Шаг 1: Клонирование репозитория

```bash
git clone git@github.com:yappiepro/website1.git
cd website1
```

### Шаг 2: Установка зависимостей (для разработки)

```bash
npm install
npm run dev
```

### Шаг 3: Сборка для продакшена

```bash
npm run generate
```

### Шаг 4: Публикация на GitHub Pages

1. Убедитесь, что папка `dist` включена в коммит (не игнорируется в .gitignore)
2. Добавьте, закоммитьте и запушьте изменения:

```bash
git add .
git commit -m "Update website build"
git push origin main
```

3. В настройках репозитория на GitHub:
   - Перейдите в **Settings** → **Pages**
   - В разделе **Source** выберите **Deploy from a branch**
   - Выберите ветку `main` и папку `/dist`
   - Нажмите **Save**

4. Подождите 2-3 минуты, пока сайт опубликуется
5. Сайт будет доступен по адресу: `https://yappiepro.github.io/website1/`

## 📁 Структура проекта

```
.
├── assets/           # Статические ресурсы (CSS, изображения)
├── components/       # Vue компоненты
├── dist/            # Собранный сайт (для GitHub Pages)
├── app.vue          # Главный компонент
├── nuxt.config.ts   # Конфигурация Nuxt
└── package.json     # Зависимости проекта
```

## 🛠 Технологии

- **Nuxt.js 3** - Vue фреймворк
- **Tailwind CSS** - CSS фреймворк
- **Vue 3** - JavaScript фреймворк
- **GitHub Pages** - Хостинг

## 📝 Примечания

- Сайт настроен на статическую генерацию (`ssr: false`)
- Базовый URL настроен для репозитория `/website1/`
- Все ссылки и ресурсы относительные

## 🆘 Поддержка

Если возникли проблемы с деплоем, проверьте:
1. Настройки GitHub Pages включены для репозитория
2. Папка `dist` не добавлена в `.gitignore`
3. Права доступа к репозиторию настроены корректно

## 🔄 Быстрое обновление сайта

Если нужно быстро обновить сайт после изменений:

```bash
# Пересобрать проект
npm run generate

# Добавить изменения в git
git add dist/
git commit -m "Update website"
git push origin main
```

Сайт автоматически обновится в течение нескольких минут.
